# calorie-counter
Скопируй этот текст, вставь в закрепленное сообщение в вашем чате (Telegram/Discord) или в `README.md` на GitHub.

-----

# 📘 ГАЙД: Как начать работать над проектом (Calorie Counter)

Парни, всем читать обязательно. Тут инструкция от "как установить программы" до "как сдать свою задачу".

## ЧАСТЬ 1. Установка (Делается 1 раз в жизни)

Если у вас чистый комп, сначала ставим инструменты.

### 1\. Редактор кода (IDE)

Нам нужен **Visual Studio Code** (не путать с фиолетовой Visual Studio). Это стандарт. если у вас уже скачана какая-то ide то пох оставляйте свою
👉 **Скачать:** [code.visualstudio.com](https://code.visualstudio.com/)

### 2\. Язык Python

Нам нужна версия Python 3.10 или новее. у меня 3.12.3 но роли особо не меняет, главное выше десятки
👉 **Скачать:** [python.org](https://www.python.org/downloads/)

⚠️ **ВАЖНО:** При установке обязательно поставьте галочку **"Add Python to PATH"** внизу окошка\! Если забудете — ничего работать не будет.

### 3\. Git (Система версий)

Чтобы скачивать и отправлять код.
👉 **Скачать:** [git-scm.com](https://git-scm.com/download/win) (Жмите "Next" на все вопросы при установке).

### 4\. Настройка Личности

Откройте терминал в ide (PowerShell или командную строку) и введите две команды, чтобы Гит знал, кто вы (используйте свои данные):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваша@почта.com"
```

-----

## ЧАСТЬ 2. Первый запуск проекта

1.  **Создайте папку** `projects` на диске C или D (чтобы путь был коротким и без русских букв).
2.  Откройте эту папку в **VS Code** (`File -> Open Folder`).
3.  Откройте терминал в VS Code (`Ctrl + ~` или `Terminal -> New Terminal`).
4.  **Клонируйте проект** (скачайте его):
    ```bash
    git clone https://github.com/MrArkashashowYo/calorie-counter.git
    ```
5.  Зайдите внутрь папки:
    ```bash
    cd calorie-counter
    ```
6.  **Создайте виртуальное окружение** (вашу личную "коробку" с библиотеками):
    ```bash
    python -m venv venv
    ```
7.  **Активируйте его** (должна появиться надпись `(venv)` слева):
    ```bash
    .\venv\Scripts\activate
    ```
    *(Если чето не получается то используйте нейронку)*
8.  **Установите библиотеки:**
    ```bash
    pip install -r requirements.txt
    ```

Всё\! Вы готовы к работе.

-----

## ЧАСТЬ 3. Алгоритм действий при выполнении задач

Каждый раз, когда садишься кодить, делай по этому списку. **В главной ветке `main` мы НЕ работаем\!**

### Шаг 1. Подготовка

1.  Открой проект в VS Code.
2.  Открой терминал.
3.  **Включи venv:** `.\venv\Scripts\activate` (должно быть `(venv)`).
4.  **Обнови проект** (скачай изменения, которые сделали другие):
    ```bash
    git pull
    ```

### Шаг 2. Создание своей ветки

Никогда не пиши код в `main`. Создай ветку под свою задачу.
Например, задача "сделать поиск":

```bash
git checkout -b feature/search-logic
```

### Шаг 3. Работа

Пишешь код, сохраняешь файлы.
Чтобы проверить, работает ли сайт:

```bash
python manage.py runserver
```

*(Остановить сервер: `Ctrl + C`)*

### Шаг 4. Сохранение (Коммит)

Когда сделал кусок работы:

1.  Добавь файлы в "посылку":
    ```bash
    git add .
    ```
2.  Подпиши посылку (понятно, что сделал):
    ```bash
    git commit -m "Добавил поиск продуктов по названию"
    ```

### Шаг 5. Отправка (Пуш)

Отправь свою ветку на GitHub:

```bash
git push
```

*(Если спросит команду `--set-upstream`, просто скопируй её из терминала и вставь).*

### Шаг 6. Сдача работы (Pull Request)

1.  Зайди на страницу проекта на GitHub.

2.  Там появится желтая плашка "Compare & pull request". Жми её.

3.  Напиши заголовок и нажми **Create Pull Request**.

4.  Кинь ссылку Аркаше. Жди, пока он примет.

-----

## ⛔ ВАЖНЫЕ ПРАВИЛА (Где трогать, где нет)

  * 🔴 **КРАСНАЯ ЗОНА (Не трогать\!):**

      * `manage.py`
      * Папки `migrations`
      * Файлы `wsgi.py`, `asgi.py`
      * Если тронете — сайт сломается у всех.
      * `calorie_counter_settings/settings.py`
      * `requirements.txt`

  * 🟢 **ЗЕЛЕНАЯ ЗОНА (Ваше место):**

      * Работайте только внутри папок приложений: `meals`, `calories`.
      * `models.py` — для таблиц данных.
      * `views.py` — для логики.
      * `templates/` — для HTML.
  ну если оно потребуется то можете и вдругом месте главное не в красной зоне
